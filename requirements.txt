# ChainTrace dependencies.
#
# Everything here is needed for the tool to run end to end offline. Deliberately small:
# each extra dependency is another thing that can fail to install on a demo laptop.

# --- data ---
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# --- model + explanation ---
scikit-learn>=1.3          # RandomForest baseline; beats GNNs on this data (Weber 2019)
joblib>=1.3

# --- graph ---
networkx>=3.0              # entity graph, typology rules, k-hop subgraph extraction

# --- API ---
fastapi>=0.110
uvicorn>=0.27

# --- tests ---
pytest>=7.4

# ---------------------------------------------------------------------------------------
# OPTIONAL -- none of these are needed to run or demo the tool.
#
#   shap>=0.44             ml/explain/attribution.py uses shap.TreeExplainer when present
#                          and falls back to an exact tree-path decomposition otherwise.
#   xgboost>=2.0           alternative to RandomForest in ml/train/baseline.py
#   lightgbm>=4.0          ditto
#   torch, torch-geometric  the GNN benchmark (role 3). Large; install only if you get
#                          to it, and expect it to lose to the forest.
#   jinja2, weasyprint     PDF case-pack export
# ---------------------------------------------------------------------------------------
